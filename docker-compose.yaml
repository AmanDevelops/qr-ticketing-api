version: "3.8"

services:
  db:
    image: postgres:15
    container_name: postgres_server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

